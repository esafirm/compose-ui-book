package nolambda.uibook.factory

class JsLibraryLoader : LibraryLoader {
    override fun load(): UIBookLibrary {
        TODO("Load the library loader from here. Is there a reflection in Kotlin JS?")
    }
}
